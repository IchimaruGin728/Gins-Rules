{
  "name": "gins-rules",
  "private": true,
  "packageManager": "pnpm@11.0.9",
  "scripts": {
    "sync": "swift run --package-path tools gins-rules-syncer --root . sync && swift run --package-path tools gins-rules-syncer --root . geo",
    "compile": "swift run --package-path tools gins-rules-compiler --root .",
    "board:build": "pnpm --filter gins-rules-board build && cp -r dashboard/dist/* compiled/",
    "build": "pnpm sync && pnpm compile && pnpm board:build",
    "dev": "wrangler dev --config cmd/scanner/wrangler.jsonc",
    "deploy": "pnpm build && wrangler deploy --config cmd/scanner/wrangler.jsonc"
  },
  "devDependencies": {
    "wrangler": "latest"
  },
  "pnpm": {
    "onlyBuiltDependencies": [
      "esbuild",
      "sharp",
      "workerd"
    ]
  }
}
